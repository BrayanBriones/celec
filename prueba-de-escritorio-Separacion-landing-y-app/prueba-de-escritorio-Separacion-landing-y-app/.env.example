# Puerto del servidor HTTP
PORT=4000

# URL del frontend autorizada para CORS
CLIENT_URL=http://localhost:5173

# Secretos para firmar y validar tokens (cambia estos valores en producción)
ACCESS_TOKEN_SECRET=dev-access-secret
REFRESH_TOKEN_SECRET=dev-refresh-secret

# Tiempos de vida de los tokens
ACCESS_TOKEN_TTL_MINUTES=20
REFRESH_TOKEN_TTL_DAYS=14
